{
  "name": "etui",
  "version": "1.0.0",
  "description": "Simple tui elemets library",
  "author": "z-Eduard005",
  "license": "MIT",
  "scripts": {
    "build": "tsup src/main.ts --format cjs --target node18 --out-dir build --treeshake --minify",
    "release": "npm run build && npm release",
    "test": "npm run build && echo test"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.41.0"
  }
}